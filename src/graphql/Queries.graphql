# GraphQL operations (queries/mutations/fragments) only — no JS/TS imports or code here.

query GetCharacters($page: Int) {
  characters(page: $page) {
    info {
      pages
      next
      prev
    }
    results {
      id
      name
      status
      species
      gender
      image
    }
  }
}
